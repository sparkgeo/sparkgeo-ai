# General Rules

* After each prompt/file editing session do a git commit with a short message explaining what you've done.  If a git repository doesn't exist, create one (`git init`)
* In the .claude directory, can you keep a transcript.md file that keeps track of my prompts and a summary of your response. If the .claude directory doesn't exist in the repository, create it.